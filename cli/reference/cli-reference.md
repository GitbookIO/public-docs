---
description: Reference for every GitBook CLI command, grouped by content commands and integration commands.
---

# CLI reference

> **Stub for tree test.**
> **Purpose:** Answers: what commands does the GitBook CLI have and what are their flags? Lookup reference, organized by command group:
>
> * **Content commands** — work on GitBook sites/spaces (`organizations`, `spaces`, `content pages`, `ask`)
> * **Integration commands** — build and publish custom components (`integration new`, `integration dev`, `integration publish`/`unpublish`)
> * **Authentication** — shared by both groups (`login`, `auth`, `whoami`, `logout`)
>
> **Canonical home of:** the full GitBook CLI command reference, across both command groups — do not duplicate elsewhere.
> **Cross-links:** → Custom components → CLI reference (thin stub pointing back here)
