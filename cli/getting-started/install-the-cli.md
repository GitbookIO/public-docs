# Install the CLI

The GitBook CLI requires Node v18 or later. Install it globally from npm:

```bash
npm install @gitbook/cli -g
```

This installs the `gitbook` command. Check it's working:

```bash
gitbook --version
```

Next, [authenticate](authentication.md) before running any commands that touch your GitBook content or organization.