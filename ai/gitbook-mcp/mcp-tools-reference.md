---
description: Reference for every tool the GitBook MCP server exposes.
---

# MCP tools reference

{% hint style="warning" %}
**Not yet written.** This page needs the actual, current list of tools the GitBook MCP server exposes — names, parameters, and descriptions — from whoever owns the MCP server implementation. Nothing in this repo's source content documents the tool list, and it shouldn't be guessed at or reverse-engineered from the workflow prompts in [Common workflows](common-workflows.md), since those describe outcomes an agent can achieve, not the specific tool calls it makes to get there.
{% endhint %}

In the meantime, the most reliable way to see the current tools is to ask a connected MCP client to list them directly (most clients expose a "list tools" or equivalent inspector view once connected — see [Connect your client](connect-your-client.md)).
