---
description: When to move an agent workflow from conversational MCP to deterministic CLI steps.
---

# CLI for agents

> **Stub for tree test.**
> **Purpose:** Answers: when should my agent's automation or CI use the CLI instead of MCP tool calls? Thin bridge into scripted, deterministic steps.
> **Canonical home of:** the narrative for using the CLI in agent/automation workflows — NOT the CLI command reference itself.
> **Cross-links:** → Developers → CLI → CLI reference (canonical command reference)
