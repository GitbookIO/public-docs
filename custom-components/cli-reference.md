---
description: The GitBook CLI reference now lives in one place — see Developers → CLI.
---

# CLI reference

> **Stub for tree test.**
> **Purpose:** Points integration builders to the one canonical CLI reference instead of duplicating it here.
> **Canonical home of:** nothing — this is a linking stub only. The canonical reference (content commands and integration commands, in one place) lives at Developers → CLI → CLI reference.
> **Cross-links:** → Developers → CLI → CLI reference (canonical, covers integration commands too)
