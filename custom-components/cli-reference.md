---
description: The GitBook CLI reference now lives in one place — see Developers → CLI.
---

# CLI reference

Looking for the CLI command reference? It lives in one place, covering both content commands and integration commands: Developers → CLI → CLI reference.
